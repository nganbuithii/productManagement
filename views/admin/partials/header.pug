header.header 
    .container-fluid
        .row.align-items-center 
            .col-3 
                .inner-logo 
                    a(href="/admin/dashboard") admin
            .col-9
                .text-right 
                    a(href=`${prefixAdmin}/my-account` class="btn btn-primary") #{user.fullName}
                    //- user là biến toàn cục
                    a(href=`${prefixAdmin}/auth/logout` class="btn btn-success") Đăng xuất
                