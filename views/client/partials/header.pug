include ../mixins/sub-menu.pug
header.header 
    .container 
        .row.align-items-center 
            
            .col-3 
                .inner-logo
                    a(href="/")
                        img(src="/images/logo.png")
            .col-5 
                form(action="/search" method="GET")
                    .form-group.d-flex.mb-0 
                        input(type="text" placeholder="Nhập từ khóa.." class="form-control" name="keyword" value=keyword)
                        button(type="submit" class="btn btn-success") Tìm kiếm
            .col-4
                .inner-menu 
                    ul 
                        li 
                            a(href="/") Trang chủ
                        li(class="sub-menu")
                            -const pathProduct =  "/products"
                            a(href=pathProduct)  Sản phẩm
                                +sub-menu(layoutProductCategory, pathProduct)
                        li 
                            a(href="/cart") Giỏ hàng (#{miniCart ? miniCart.totalQuantity : "0"})
                            //- bời vì header đi xuyên suốt tất cả các trang vì thế phải viết vào middleware
                            //- để giỏ hàng là datta động
                        li 
                            a(href="/user/register") Đăng ký
                        li 
                            a(href="/user/login") Đăng nhập